# ================================================================
# REQUIREMENTS.TXT - PINN Framework v29.43 (Full Development)
# ================================================================
# هذه النسخة تشمل جميع المكتبات اللازمة للتطوير المحلي،
# بما في ذلك المكتبات الاختيارية لمقارنة النماذج والتصورات الإضافية.
# يمكنك إزالة أي مكتبة مكتوب بجانبها "(اختياري)" إن لم تكن بحاجتها.
# ================================================================

# --- المكتبات الأساسية (Core Libraries) ---
streamlit==1.35.0          # إطار عمل واجهة المستخدم التفاعلية
torch==2.3.0               # PyTorch - بناء وتدريب الشبكات العصبية
numpy==1.26.4              # العمليات الحسابية على المصفوفات
pandas==2.2.2              # معالجة البيانات (DataFrames)
scikit-learn==1.5.0        # أدوات التعلم الآلي (مقاييس، تحجيم، تقسيم البيانات)
plotly==5.22.0             # الرسوم البيانية التفاعلية (Pareto, Sensitivity, إلخ)
fpdf==1.7.2                # إنشاء تقارير PDF
scipy==1.13.0              # العمليات العلمية (الاستيفاء، التكامل) - مطلوب للـ Smooth Curve

# --- نماذج إضافية للمقارنة (اختيارية) ---
xgboost==2.1.0             # نموذج XGBoost للمقارنة في جدول الأداء (اختياري)
matplotlib==3.8.0          # الرسوم الأساسية (احتياطي) (اختياري)
seaborn==0.13.0            # تحسين الرسوم الإحصائية (احتياطي) (اختياري)

# --- أدوات مساعدة (اختيارية) ---
pickle5                    # لحفظ وتحميل النماذج (في حال الحاجة)
